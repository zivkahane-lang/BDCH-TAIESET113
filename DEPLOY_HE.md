# איך מעלים את האתר ל-GitHub Pages (חינם, 10 דקות)

## שלב 1 - יצירת חשבון ורפוזיטורי
1. אם אין לך חשבון: היכנסי ל-github.com והירשמי (חינם).
2. לחצי על "+" למעלה מימין -> "New repository".
3. תני שם, למשל: `bdch-taiseset` (באנגלית, בלי רווחים).
4. סמני "Public" (חשוב כדי ש-GitHub Pages יעבוד בחינם), ולחצי "Create repository".

## שלב 2 - העלאת הקבצים
1. באותו דף של הרפוזיטורי, לחצי על "uploading an existing file".
2. גררי לתוכו את **כל** הקבצים והתיקיות מתוך התיקייה שקיבלת ממני
   (index.html, style.css, manifest.json, sw.js, briefing.html,
   trainings.html, tests.html, notes.html, התיקייה icons/, התיקייה assets/).
3. לחצי "Commit changes".

## שלב 3 - הפעלת GitHub Pages
1. בתוך הרפוזיטורי, לכי ל-"Settings" (בתפריט העליון).
2. בתפריט הצד, לחצי "Pages".
3. תחת "Branch", בחרי `main` ותיקייה `/ (root)`, ולחצי "Save".
4. חכי כדקה-שתיים. יופיע קישור בסגנון:
   `https://<שם-המשתמש-שלך>.github.io/bdch-taiseset/`
   זו הכתובת של האתר החי שלך.

## שלב 4 - השלמת התוכן האמיתי
לפני שמפיצים את הקישור לטייסת, כדאי להשלים:
- **הבד"ח**: להעלות את קובץ ה-PPTX האמיתי לתיקיית `assets/` בשם `bdch.pptx`
  (או להחליף את הקישור ב-briefing.html לקישור גוגל דרייב).
- **הדרכות**: להעלות את שלושת קובצי ה-MP4 לתיקיית `assets/videos/`
  (ראו את השמות המדויקים בקובץ assets/README.txt).
- **מבחנים**: לפתוח את tests.html ולהחליף את שלושת הקישורים
  (`REPLACE_ME_1/2/3`) בקישורי מסמכי הגוגל דוקס האמיתיים.
- **הערות פרטיות**: לפתוח את notes.html ולשנות את קוד הגישה
  (המחרוזת `"1234"` בתוך התג `<script>`) לקוד לבחירתך.

בכל פעם שמעדכנים קובץ ב-GitHub (Commit), האתר מתעדכן אוטומטית
תוך דקה-שתיים - אין צורך "לפרסם" שוב.

## שלב 5 - הוספה למסך הבית בטלפון
1. פתחי את הקישור של האתר בדפדפן בטלפון (Safari באייפון / Chrome באנדרואיד).
2. אייפון: לחצי על כפתור השיתוף (הריבוע עם החץ) -> "הוסף למסך הבית".
   אנדרואיד: לחצי על שלוש הנקודות -> "הוספה למסך הבית" / "התקן אפליקציה".
3. יופיע קיצור דרך עם הלוגו של הטייסת, שנפתח ישירות למסך מלא כמו אפליקציה.

## חשוב לדעת
- זה אתר **ציבורי** אם משאירים את הרפוזיטורי Public (כל מי שיש לו את
  הקישור המדויק יכול לגשת, אבל הוא לא מופיע בגוגל אלא אם מישהו מקשר אליו).
  אם צריך פרטיות מלאה - יש אפשרות ל-Private repo + GitHub Pages בתשלום,
  או אירוח אצל ספק אחר עם הגנת סיסמה אמיתית בצד השרת.
- דף "הערות אישיות" מוגן בקוד פשוט בצד הדפדפן בלבד - טוב למניעת מבטים
  מזדמנים, אבל **לא הצפנה אמיתית**. ההערות נשמרות רק במכשיר שבו כתבת
  אותן (localStorage), ולא מסתנכרנות בין מכשירים.
